{
  "name": "xiangqi",
  "version": "1.4.0",
  "private": true,
  "description": "中国象棋 · 小而美：棋盘为主、三档 AI、离线语音、安静暗色界面",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint js/ tests/ scripts/",
    "tts:generate": "node scripts/generate-tts.mjs",
    "tts:force": "node scripts/generate-tts.mjs --force",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:build:debug": "tauri build --debug",
    "desktop:shortcut": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/create-desktop-shortcut.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "eslint": "^9.25.0",
    "node-edge-tts": "^1.2.10",
    "vite": "^6.3.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.1"
  }
}
